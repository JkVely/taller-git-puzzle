[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "project-phoenix"
version = "1.1.0"
description = "Collaborative game engine project"
requires-python = ">=3.10"
